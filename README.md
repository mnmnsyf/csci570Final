# csci570Final
The project is on the implementation of the two different solutions discussed in class for the Sequence Alignment problem.

## Dependencies

This project uses Python standard library modules plus `psutil` for memory measurement. No other external Python libraries are allowed.
